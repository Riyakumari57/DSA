class Solution {
public:
    int xorOperation(int n, int start) 
    {
        int num[n];
        for(int i=0;i<n;i++)
        {
            num[i] =  start + 2*i;
        }
        int x = 0;
        for(int i =0;i<n;i++)
        {
            x = x^num[i];
        }
        return x;
    }
};
